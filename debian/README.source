This package does not carry the upstream branches nor libraries. To generate
them, you can use the following steps:

1. Call `origtargz` which will download the upstream release using uscan.
2. Call `tar xf ../qcom-adreno_1.855.5.orig.tar.gz --strip-components=1`
   to extract the upstream `data/` folder in the current directory.
